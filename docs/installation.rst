============
Installation
============

At the command line::

    $ easy_install dj-notifications

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-notifications
    $ pip install dj-notifications
